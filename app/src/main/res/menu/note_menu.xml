<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/mi_note_color"
        android:title="@string/note_change_color"
        android:iconTint="@color/background"
        android:icon="@drawable/ic_palette_24"
        app:showAsAction="always"/>

    <item android:id="@+id/mi_note_delete"
        android:title="@string/note_delete"
        android:iconTint="@color/background"
        android:icon="@drawable/ic_delete_24"
        app:showAsAction="ifRoom"/>
</menu>